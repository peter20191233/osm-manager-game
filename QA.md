# Проверка игры

Проверено 13 сентября 2026 года.

- **24 автоматических теста пройдены**: начисление +1/−1, отрицательный рейтинг, повторные нажатия, тайм-аут, пауза, тренировка, завершение смены, воспроизводимость и баланс очереди, корректность сценариев и источников, безопасная раздача файлов сервером.
- В браузере пройдена полная тренировка из 12 клиентов: 11 верных ответов, 1 ошибка, итог **10 очков**, **92%**, лучшая серия **11**. Разбор ошибки соответствует запросу клиента.
- Проверены справочник после ответа и возврат к следующему клиенту, перезапуск, переключение режимов, ручная пауза и штраф за истечение 35 секунд.
- Проверено представление 390 × 844: горизонтальной прокрутки нет, все шесть кнопок доступны, высота каждой — 70 px. После ответа интерфейс прокручивается к объяснению. Это проверка адаптивной вёрстки в Chromium, а не испытание на физических iPhone/Android.
- Автономный HTML проверен через локальный HTTP: Python запускается, правильный талон даёт +1. Все игровые ресурсы встроены. Проверяющий браузер блокирует навигацию по `file://`, поэтому прямой запуск двойным щелчком этим инструментом не проверялся.
- **Офлайн-проверка PWA пройдена**: после заполнения кэша `osm-game-v4` сеть вкладки отключена, страница перезагружена, смена начата и правильный талон выдан (+1). Затем сеть восстановлена. Кэш требует HTTPS или localhost; для HTTP-адреса компьютера в Wi-Fi это свойство не заявляется.
- Оба CMD проверены с `--help`. Текущая сборка с музыкой создаёт автономный HTML ~2,2 МБ и ZIP ~1,8 МБ, включая исходники, тесты и лицензию Brython.

Основная команда повторной проверки:

```powershell
py -3 -m unittest discover -s tests -v
```

Снимок мобильной проверки: [qa/mobile-game.png](qa/mobile-game.png).

## Android 1.1.0 и музыка

Подписанный APK: **947,735 байт**. [Сборка и проверки](https://github.com/peter20191233/osm-manager-game/actions/runs/34763211480).

- Android 12 / API 31 и Android 15 / API 35: по **4 инструментальных теста**, Wi-Fi и мобильная сеть выключены до установки.
- Полная смена, +1/−1, защита от двойного ответа, рекорд после перезапуска, пауза при уходе в фон.
- Встроенный MP3 действительно декодируется и воспроизводится: длительность более 79 секунд, продвижение времени, зацикливание, mute/unmute, остановка при паузе и уходе в фон.
- Проверены SHA-256 установленного APK, прежний сертификат, отсутствие debuggable и INTERNET. Файлы приложения в двух проверенных APK совпадают.
- 24 теста Python и Android lint проходят. Тестовая инфраструктура не включена в APK.

SHA-256 APK: `051ddca9ea4fdc5959d81335f666545971bd29b8df113641b3018c9d4d658cb7`.

SHA-256 сертификата: `24280b33753e25bb2e90ae4af85d5862da25c44b72be76ddf86a534a58a16f7e`.

## Веб-музыка

[WebKit 26.5 на macOS: успешный запуск](https://github.com/peter20191233/osm-manager-game/actions/runs/34763341429).
Chromium 151 проверен на Windows. В обоих движках проверены музыка и полный игровой цикл
при выключенном локальном сервере, mute/unmute, пауза, рекорд и запуск после закрытия браузера.
Проверены ширины 320, 390 и 844 px. Ошибок JavaScript нет.

WebKit на Windows не завершал загрузку встроенного MP3; медиапроверка поэтому выполнена
в WebKit на macOS. Это не проверка установки на физическом iPhone. Физические Android-устройства также не проверялись.

[Android-отчёт](qa/android-release.json) · [Веб-отчёт](qa/ios-webapp.json).
